Escape Encoding
идея:
-Последовательности одинаковых символов заменяются на специальные маркеры
-Символ # используется как служебный и кодируется особым образом

Алгоритм:
1. Проход по строке и поиск последовательностей одинаковых символов
2. Для обычных символов:

Если последовательность > 259 символов - разбивается на блоки по 259

Если последовательность ≥ 4 символов - кодируется как #<length-4><symbol>

Меньшие последовательности записываются как есть
3. Для символа #:

Если последовательность > 256 символов - разбивается на блоки по 256

Кодируется как #<length-1>#

Escape Decoding
Алгоритм:
1. Поиск маркера #
2. Если найден:

Следующий символ - длина последовательности

Затем - символ для повторения

Для #: length = count + 1

Для других: length = count + 4
3. Если маркер не найден - символ копируется как есть


Jump Encoding
Принцип работы:
-Разделяет данные на "уникальные последовательности" и "повторяющиеся символы"
-Использует разные методы кодирования для каждого типа

JumpEncode
Алгоритм:
1. Разделение данных на:

Уникальные последовательности (длиной 1-2 символа)

Повторяющиеся символы (3 и более)
2. Кодирование повторений:

Максимальный блок - 130 символов

Формат: <length-3><symbol>
3. Кодирование уникальных последовательностей:

Максимальный блок - 128 символов

Формат: <length+127><sequence>



JumpDecode
Алгоритм:
1. Чтение первого байта:

-Если значение > 127 - это уникальная последовательность

Длина = значение - 127

Следующие length символов копируются как есть

-Если ≤ 127 - это повторения

Длина = значение + 3

Следующий символ повторяется length раз

